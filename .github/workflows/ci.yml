name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build & Test (macOS ${{ matrix.macos }} - ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS 13 Ventura - Intel (native)
          - macos: "13"
            runner: macos-13
            arch: x64
          # macOS 13 Ventura - Apple Silicon (large runner)
          - macos: "13"
            runner: macos-13-xlarge
            arch: arm64
          # macOS 14 Sonoma - Intel (large runner)
          - macos: "14"
            runner: macos-14-large
            arch: x64
          # macOS 14 Sonoma - Apple Silicon (native)
          - macos: "14"
            runner: macos-14
            arch: arm64
          # macOS 15 Sequoia - Intel (large runner)
          - macos: "15"
            runner: macos-15-large
            arch: x64
          # macOS 15 Sequoia - Apple Silicon (native)
          - macos: "15"
            runner: macos-15
            arch: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "5.9"

      - name: Show Swift version
        run: swift --version

      - name: Build
        run: swift build -v

      - name: Run tests
        run: swift test -v

  # Summary job that depends on all matrix jobs
  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: build-and-test
    if: always()
    steps:
      - name: Check all jobs passed
        run: |
          if [ "${{ needs.build-and-test.result }}" != "success" ]; then
            echo "Build and test failed"
            exit 1
          fi
          echo "All CI checks passed!"
